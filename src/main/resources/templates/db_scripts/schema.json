{
  "scripts": [
    {
      "run": "after",
      "snippet": "CREATE TABLE IF NOT EXISTS custom_fields(refId VARCHAR(50) PRIMARY KEY , jsonb JSONB NOT NULL);",
      "fromModuleVersion": "1.0"
    }
  ]
}
